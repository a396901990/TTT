<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <View
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_above="@+id/button_layout"
        android:background="#33ffffff" />


    <LinearLayout
        android:layout_marginBottom="20dp"
        android:layout_alignParentBottom="true"
        android:id="@+id/circles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal"/>

    <RelativeLayout
        android:visibility="gone"
        android:id="@+id/button_layout"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/welcome_bottom_height"
        android:layout_alignParentBottom="true"
        android:background="@android:color/transparent">

        <ImageButton
            android:id="@+id/btn_pre"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="@dimen/welcome_bottom_btn_width"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:gravity="center"
            android:paddingBottom="@dimen/welcome_bottom_btn_padding"
            android:paddingTop="@dimen/welcome_bottom_btn_padding"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_action_pan_left"
            android:visibility="invisible" />

        <!--<LinearLayout-->
            <!--android:id="@+id/circles"-->
            <!--android:layout_width="wrap_content"-->
            <!--android:layout_height="wrap_content"-->
            <!--android:layout_centerInParent="true"-->
            <!--android:orientation="horizontal">-->

        <!--</LinearLayout>-->

        <Button
            android:id="@+id/done"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="@dimen/welcome_bottom_btn_width"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:gravity="center"
            android:text="@string/welcome_done"
            android:textColor="@color/white"
            android:textSize="@dimen/text_large"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/btn_next"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="@dimen/welcome_bottom_btn_width"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:gravity="center"
            android:paddingBottom="@dimen/welcome_bottom_btn_padding"
            android:paddingTop="@dimen/welcome_bottom_btn_padding"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_action_pan_right" />
    </RelativeLayout>

</RelativeLayout>
